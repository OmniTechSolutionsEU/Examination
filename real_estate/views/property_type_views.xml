<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Property Type List View -->
    <record id="view_property_type_tree" model="ir.ui.view">
        <field name="name">property.type.tree</field>
        <field name="model">property.type</field>
        <field name="arch" type="xml">
            <list string="Property Types">
                <field name="name"/>
                <field name="property_count" sum="Total Properties"/>
            </list>
        </field>
    </record>

    <!-- Property Type Form View -->
    <record id="view_property_type_form" model="ir.ui.view">
        <field name="name">property.type.form</field>
        <field name="model">property.type</field>
        <field name="arch" type="xml">
            <form string="Property Type">
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                        </group>
                    </group>
                    <group>
                        <field name="description" placeholder="Enter description..."/>
                    </group>
                    <notebook>
                        <page string="Properties" name="properties">
                            <!-- One2many relationship (reverse of Many2one) -->
                            <field name="property_ids" readonly="1">
                                <list>
                                    <field name="name"/>
                                    <field name="postcode"/>
                                    <field name="expected_price"/>
                                    <field name="state"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Property Type Search View -->
    <record id="view_property_type_search" model="ir.ui.view">
        <field name="name">property.type.search</field>
        <field name="model">property.type</field>
        <field name="arch" type="xml">
            <search string="Search Property Types">
                <field name="name"/>
                <filter string="With Properties" name="filter_with_properties" 
                        domain="[('property_ids', '!=', False)]"/>
            </search>
        </field>
    </record>

    <!-- Property Type Action -->
    <record id="action_property_type_list" model="ir.actions.act_window">
        <field name="name">Property Types</field>
        <field name="res_model">property.type</field>
        <field name="view_mode">list,form</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first property type!
            </p>
            <p>
                Property types help categorize your properties (e.g., House, Apartment, Commercial).
                This demonstrates Many2one relationships.
            </p>
        </field>
    </record>
</odoo>

